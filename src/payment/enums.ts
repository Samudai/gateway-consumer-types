export enum ProviderType {
  'wallet' = 'wallet',
  'gnosis' = 'gnosis',
  'parcel' = 'parcel',
}

export enum PaymentStatus {
  PENDING = 'pending',
  PAID = 'paid',
  FAILED = 'failed',
}

export enum WalletType {
  ETH = 0,
  SOL = 1,
}