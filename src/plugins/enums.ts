export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in_progress',
  DONE = 'done',
  IN_REVIEW = 'in_review',
  BACKLOG = 'backlog',
}

export enum PluginType {
  DISCORD = 'discord',
  GNOSIS = 'gnosis',
  SNAPSHOT = 'snapshot',
  GCAL = 'gcal',
  NOTION = 'notion',
  TWITTER = 'twitter',
}
